/**
 * @module ocpp-ws-io/logger
 *
 * Re-exports the `voltlog` package for convenience.
 *
 * Users who already have `ocpp-ws-io` installed can use:
 * ```ts
 * import { createLogger } from 'ocpp-ws-io/logger';
 * ```
 *
 * Instead of installing `voltlog` separately.
 */
export * from "voltlog";
